{
  "name": "happy-kid-client",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix happy-kid-backend && npm install --prefix happy-kid-client && npm run build --prefix happy-kid-client"
  }
}